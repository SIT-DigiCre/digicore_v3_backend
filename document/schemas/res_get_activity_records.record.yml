required:
  - recordId
  - userId
  - username
  - place
  - checkedInAt
  - initialCheckedInAt
properties:
  recordId:
    type: string
  userId:
    type: string
  username:
    type: string
  place:
    type: string
  checkedInAt:
    type: string
    format: date-time
  checkedOutAt:
    type: string
    format: date-time
    nullable: true
  initialCheckedInAt:
    type: string
    format: date-time
  initialCheckedOutAt:
    type: string
    format: date-time
    nullable: true
