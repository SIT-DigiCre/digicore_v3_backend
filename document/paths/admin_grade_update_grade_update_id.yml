put:
  tags:
    - grade-update
  description: "学年補正申請の承認/却下（管理者用）"
  security:
    - BearerAuth:
        - "infra"
  parameters:
    - name: gradeUpdateId
      in: path
      required: true
      schema:
        type: string
  requestBody:
    description: "承認/却下リクエスト"
    required: true
    content:
      application/json:
        schema:
          $ref: "../schemas/req_put_admin_grade_update_grade_update_id.yml"
  responses:
    200:
      $ref: "../responses/blank_success.yml"
    400:
      $ref: "../responses/bad_request.yml"
    403:
      $ref: "../responses/unauthorized.yml"
    404:
      $ref: "../responses/not_found.yml"
    500:
      $ref: "../responses/internal_server.yml"
