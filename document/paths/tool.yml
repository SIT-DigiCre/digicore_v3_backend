get:
  tags:
    - tool
  description: "Get tool URL"
  responses:
    200:
      description: "Tool URL"
      content:
        application/json:
          schema:
            $ref: "../schemas/res_get_tool.yml"
