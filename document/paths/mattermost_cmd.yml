post:
  tags:
    - mattermost
  description: "Mattermost slash cmd"
  requestBody:
    description: "Slash cmd request"
    required: true
    content:
      application/json:
        schema:
          $ref: "../schemas/req_post_mattermost_cmd.yml"
  responses:
    200:
      description: "Success slash cmd"
      content:
        application/json:
          schema:
            $ref: "../schemas/res_post_mattermost_cmd.yml"
    500:
      $ref: "../responses/internal_server.yml"
